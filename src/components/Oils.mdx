export const OilImage = ({src, alt}) => (
    <img src={src} alt={alt} style={{height: 100, imageRendering: "pixelated"}}/>
)

export const Oil = ({children, title, image, source}) => {
    return (
        <div style={{ marginTop: '15px'}} id={title.toLowerCase().replaceAll(' ','-')}>
            <div className="container" style={{ display: 'flex', alignItems: 'center', marginLeft: '-20px'}}>
                <div className="image">
                    <OilImage src={image} alt={title} />
                </div>
                <div className="text" style={{ marginLeft: '10px'}}>
                    <h2>{title}</h2>
                </div>
            </div>
            <span>
                {children}
            </span>
            <div>
                {source != null && <div style={{ marginTop: '20px'}}>
                    <details>
                        <summary>Recipe</summary>
                        <img src={source} style={{ width: 500, imageRendering: "pixelated"}} />
                    </details>
                </div>}
            </div>
        </div>
    );
}